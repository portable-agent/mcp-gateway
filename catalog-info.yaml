apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
    name: mcp-gateway
    description: Безопасный маршрутизатор вызовов между workflow и MCP-сервисами
    annotations:
        github.com/project-slug: portable-agent/mcp-gateway
        backstage.io/techdocs-ref: dir:.
spec:
    type: service
    lifecycle: experimental
    owner: group:default/backend
    system: portable-agent
